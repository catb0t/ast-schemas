# two kinds of scope rules:
# LuaBrat-style standard lexically-derived block scoping
# simpler explicit-only scoping, where blocks can only access the global
#   objects (object, number, etc) and their arguments
